<?xml version="1.0" encoding="UTF-8"?>
<document>
    <properties>
        <title>Plugin indicator</title>
    </properties>
    <body>
        <section name="Plugin indicator">
           <subsection name="Introduction">
                <p>This plugin allows you to manage a database of indicators with historical values.</p>

                <p>History is managed in multiple schema (day, week, month).</p>

                <p>An API (<code>IndicatorFetcher</code> interface) defines the components loaded to update the indicator values.</p>

                <p>These components are automatically detected from the time they are declared in a Spring context file.</p>

                <p>A daemon calls at periodical interval these components. </p>
            </subsection>
        </section>
    </body>
</document>

